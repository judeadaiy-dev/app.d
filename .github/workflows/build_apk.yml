name: Force Build APK

on:
  push:
    branches: [ main ] # تأكدي من تطابق اسم الفرع
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Get dependencies
        run: flutter pub get

      - name: Force Build APK
        run: |
          flutter build apk --release --target-platform android-arm64 --no-tree-shake-icons --split-per-abi --android-skip-build-dependency-validation

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: my-app-apks
          path: build/**/outputs/flutter-apk/*.apk
